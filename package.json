{
	"name": "wc-info",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"update": "ncu -u && npm install"
	},
	"devDependencies": {
		"wrangler": "^4.42.0",
		"custom-elements-manifest": "2.1.0"
	}
}